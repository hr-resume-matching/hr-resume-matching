# 人資履歷職缺媒合決策系統 設定教學
### 完全不懂程式、不懂IT也沒關係，跟著這份文件一步一步做就好

版本：v4.5.11
適用對象：人資、行政、主管本人操作，**不需要會寫程式、不需要懂「網站」是什麼**。

---

## 這份系統是什麼？在哪裡用？

這是一個放在網路上的網頁工具，你打開瀏覽器（例如 Chrome）輸入一個網址，
就可以上傳履歷 PDF、貼上職缺說明，讓 AI 幫忙比對配分、產生推薦名單和面談問題。

**你只需要做兩件事，系統就能上線：**
1. 把這個資料夾放到一個叫「Netlify」的免費平台上（15 分鐘，跟著下面步驟做，全程滑鼠點一點）。
2. 準備一組你自己的 AI 服務金鑰（例如 Anthropic / Claude 的 API Key），貼到系統設定裡。

做完這兩步，你和你的同事之後只要打開網址就能用，**不需要每次都重新設定**。

---

## 名詞先看過一遍（看不懂沒關係，做的時候對照就好）

| 名詞 | 白話說明 |
|---|---|
| Netlify | 一個「幫你把資料夾放上網路」的免費平台，不需要懂程式 |
| 部署（deploy） | 把檔案放上網路、變成一個可以打開的網址，這個動作叫「部署」 |
| API Key（金鑰） | 一長串英數字密碼，證明「這是你自己的帳號」，用來讓 AI 幫你分析。**不是這套系統的密碼，是你另外去 AI 公司網站申請的** |
| Netlify Identity | Netlify 提供的「登入功能」，讓你知道誰用Google帳號登入過這個系統 |

---

## 步驟一、把系統放上網路（第一次設定，約15分鐘）

### 方法A：最簡單——直接把資料夾拖上去（推薦給完全不懂電腦的人）

1. 打開瀏覽器，前往：**https://app.netlify.com/**
2. 點選「Sign up」註冊一個免費帳號（可以直接用 Google 帳號登入，不用另外設密碼）。
3. 登入後，會看到一個畫面寫著類似「Want to deploy a new site without connecting to Git?」，
   或是有一個**大大的虛線方框**，上面寫「Drag and drop your site output folder here」。
4. 把你收到的這個資料夾（`hr-resume-matching-mvp-v4.5.11`）**整個資料夾**用滑鼠拖進那個虛線方框裡。
5. 等待約30秒到1分鐘，畫面會顯示「Site deploy in progress」，跑完後會出現一個網址，
   長得像 `https://隨機英文字-隨機數字.netlify.app`。
6. 點一下那個網址，就能打開系統了！**先把這個網址存起來或設成書籤**，之後同事也是打開這個網址使用。

> 小提醒：用「拖拉資料夾」的方式部署，之後如果我方（開發者）有新版本要更新，
> 你需要重新拖拉一次新的資料夾上去覆蓋，網址不會變。

### 方法B：如果你們公司IT比較懂技術，想用GitHub自動部署

這個方式適合IT人員操作，如果你不熟悉GitHub，直接用方法A即可，效果完全一樣。

1. 把整個資料夾上傳到一個新的GitHub repository。
2. 在 Netlify 選擇「Import from Git」，連接該 repository。
3. Build command 留空、Publish directory 填 `.`（因為 `netlify.toml` 裡已經寫好了設定，不需要額外填）。
4. 按「Deploy site」即可。

### 幫網址取一個好記的名字（選做，不影響功能）

部署完成後，在 Netlify 後台左側選單找到「Site configuration」→「Change site name」，
可以把亂數網址改成例如 `你的公司名-hr-matching.netlify.app`，方便同事記憶。

---

## 步驟二、開啟「Google 登入」功能（選做，但建議做）

這個功能可以讓你知道「哪些人、什麼時候」登入過這套系統，方便管理誰在使用。
**不開啟這個功能，系統一樣可以正常上傳履歷、分析職缺，只是不會記錄登入名單。**

1. 在 Netlify 後台，進入你剛剛部署的網站，左側選單點「Identity」。
2. 點「Enable Identity」啟用。
3. 往下找到「Registration preferences」，建議選「Invite only」（只有你邀請的人才能登入，
   避免不相關的人跑來登入你的系統）。
4. 往下找到「External providers」，點「Add provider」選擇「Google」，照畫面指示啟用即可
   （不需要自己申請Google開發者帳號，Netlify已經內建處理好了）。
5. 回到「Identity」頁籤，點「Invite users」，輸入你和同事的Email，對方會收到一封邀請信。
6. 對方收到信、點連結完成一次性設定後，之後打開系統網址，右上角按「使用 Google 登入」即可。

---

## 步驟二之二、設定管理者名單與企業履歷封存（v4.5.10 新增，選填）

這是給「企業想要有一個管理後台，可以看到所有人的登入紀錄、甚至事後調閱應徵者履歷原始檔」的公司使用。
**不設定這一步，系統其他功能完全不受影響**，履歷一樣只在使用者自己的瀏覽器裡處理、不會上傳保存。

### 先設定「誰是管理者」（強烈建議：不設定就沒有人能看管理後台，而不是所有人都能看）

1. 在 Netlify 後台，進入你的網站 → 左側選單「Site configuration」→「Environment variables」。
2. 新增一筆變數，Key 填 `ADMIN_EMAILS`，Value 填你（和其他管理者）的 Google 登入 email，
   多人用「逗號」分隔，例如：`boss@company.com,hr-manager@company.com`（不要留空格前後）。
3. 儲存後，需要重新部署一次網站（Netlify 後台「Deploys」頁籤點「Trigger deploy」）設定才會生效。
4. 之後只有這份名單裡的 email，登入後才能打開 `admin-login-log.html` 這個管理後台網址看到資料；
   其他同事登入後會看到「此頁面僅限管理者查看」的提示，看不到任何人的登入紀錄或履歷。

### 管理後台可以看到什麼

打開系統網址後面加上 `/admin-login-log.html`（例如 `https://你的網址.netlify.app/admin-login-log.html`），
用管理者帳號登入後，有兩個分頁：
- **登入紀錄**：全公司所有登入過的人，含姓名、Email、登入時間、IP位址、瀏覽器。
- **履歷封存**：只有「上傳者當下有勾選啟用企業封存」的履歷才會出現在這裡，可以直接點「下載」還原成原始PDF/HTML檔案。

### 開啟履歷封存（在主系統操作，每位使用者自行決定要不要開）

1. 打開主系統網址，Step 1 頁面找到「企業進階功能：履歷封存與登入稽核」卡片。
2. 勾選「啟用企業履歷封存與登入稽核」，並確認已用Google帳號登入（卡片下方會即時顯示目前狀態）。
3. 啟用後，之後上傳的每份履歷都會多一個「已封存」／「封存中」／「封存失敗」的小標籤，
   代表這份履歷檔案是否已經成功同步保存到企業後台。

> ⚠️ **重要提醒（個資法遵）**：啟用這個功能代表應徵者的完整履歷原始檔會集中保存在企業伺服器上，
> 不再只是「處理完就從瀏覽器記憶體消失」。請貴公司自行依當地個資法規（例如台灣個資法）規劃：
> 保存期限多久、要不要在應徵者投遞履歷時告知、以及只有哪些人有權限存取／下載。
> 本功能只負責技術上的保存與稽核，不能取代企業自己的個資治理政策。

---



**這一步非常重要：這套系統本身不含任何AI服務費用，你需要有自己的 Anthropic（Claude）、
Google（Gemini）或 OpenAI（ChatGPT）帳號，並且該帳號裡要有可用額度。**
金鑰只會存在你自己瀏覽器分頁的暫存記憶體裡，關閉分頁就會消失，不會上傳到任何伺服器，
**下次打開網站需要重新貼一次**（這是刻意設計的安全機制，避免金鑰外洩）。

### 如果你要用 Claude（建議首選，本系統預設使用此服務）

1. 前往 **https://console.anthropic.com/** 並註冊/登入帳號。
2. 進去後找到左側選單「API Keys」，點「Create Key」，取個名字（例如「HR媒合系統」），
   建立後會出現一長串以 `sk-ant-` 開頭的字串，**立刻複製起來**（畫面關掉後就看不到了）。
3. 確認帳號裡有付款方式或額度（Anthropic 主控台的「Billing」頁面可以加值/綁信用卡）。
4. 回到人資媒合系統網頁，點右上角「API 設定」，貼上剛剛複製的金鑰，按「啟用」。

### 如果你要用 Gemini（Google）或 ChatGPT（OpenAI）

系統的「API 設定」視窗裡有分頁籤可以切換，操作方式類似：
到該公司的開發者網站申請一組 API Key、複製、貼到對應分頁、按啟用。
金鑰格式系統會自動幫你檢查（例如 Gemini 需以 `AIza` 開頭），貼錯格式會提醒你。

> 小提醒：不同AI服務會依「用量」向你的帳號收費（不是月租），
> 費用多寡取決於你上傳幾份履歷、幾個職缺，一般測試用量費用很低，正式大量使用前建議先詢問公司財務或IT同事是否需要統一控管額度。

---

## 步驟四、開始使用（日常操作，給人資同仁）

1. 打開系統網址 → （若有開啟登入）用Google帳號登入 → 右上角確認「API 設定」顯示已啟用。
2. 「步驟一：上傳履歷」把PDF履歷拖進虛線框（一次最多10份）。
3. 「步驟二：輸入職缺」貼上職缺說明文字；如果職缺是刊登在104，
   可以在職缺卡片裡貼上104網址試試「從104自動擷取」——**如果擷取失敗**（104有防止程式抓取的保護，
   不保證每次都成功），直接把104頁面整段複製貼到職缺說明欄位即可，系統會自動幫你抓出職缺名稱。
4. 按「開始分析」，等待進度跑完（履歷和職缺數量越多，等待時間越長）。
5. 完成後可以在「董事長／總座摘要」「部門主管檢視」「HR操作明細」「候選人追蹤看板」幾個分頁切換查看，
   也可以按右上角列印功能輸出報告。

---

## 常見問題

**Q：畫面說「empty_response」或「回應中沒有文字內容」怎麼辦？**
系統已設計成會自動重試幾次。若仍然失敗，通常是AI服務當下比較忙碌，稍等1-2分鐘後再按一次「開始分析」即可，
之前已經跑完的部分會自動保留、不用重新開始。

**Q：忘記部署的網址了怎麼辦？**
登入 https://app.netlify.com/ ，在「Sites」列表可以看到你部署過的網站與網址。

**Q：可以同時給很多同事用嗎？**
可以，大家共用同一個網址即可。但每個人瀏覽器要自己貼一次AI金鑰
（可以全公司共用同一組金鑰，或是每人申請自己的，看你們公司怎麼管理額度方便）。

**Q：要花錢嗎？**
Netlify 部署本身（本系統這種用量）通常在免費額度內。真正會產生費用的是AI服務本身依用量計費，
費用高低完全取決於使用量，不是這套系統收費。

**Q：以後系統有新版本，要怎麼更新？**
收到開發者提供的新版資料夾後，回到 Netlify 後台，一樣把新資料夾拖拉進部署區塊覆蓋即可，
網址、已設定的Identity登入名單都不會不見。

---

有任何操作上的問題，可聯繫系統開發者：游士賢（felix670131@gmail.com）
